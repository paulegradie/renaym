# Stripe Configuration
STRIPE_SECRET_KEY=sk_test_...
STRIPE_PUBLISHABLE_KEY=pk_test_...
STRIPE_WEBHOOK_SECRET=whsec_...

# Stripe Price IDs (create these in Stripe Dashboard)
# 1-Year License: $12/year recurring
STRIPE_PRICE_ID_ANNUAL=price_...
# 2-Year License: $20/2 years recurring
STRIPE_PRICE_ID_2YEAR=price_...
# Lifetime License: $35 one-time payment
STRIPE_PRICE_ID_LIFETIME=price_...

# Site URL (for redirects after checkout)
NEXT_PUBLIC_URL=http://localhost:3000

# API Gateway URL (AWS Lambda backend)
# Set this to your deployed API Gateway endpoint
# Leave as localhost for local development with Next.js API routes
NEXT_PUBLIC_API_URL=http://localhost:3000

# Optional: RSA Private Key for signed license keys (base64 encoded)
# Generate using: openssl genrsa -out private_key.pem 2048
# Then base64 encode: base64 -i private_key.pem
# Leave empty to use simple license keys
LICENSE_PRIVATE_KEY=

